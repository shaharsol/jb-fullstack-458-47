function doAnotherThing(a: number, b: number): number {
    return a + b;
}

export default doAnotherThing;

export function isSomething(): boolean {
    // return 1; //error
    return true;
}

function ivan (): any {
    return '1' ? true : false;
}

// if (1) <- in javascript this is true
// if (0) <- in javascript this is false

